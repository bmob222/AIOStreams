{
  "buildCommand": "cd packages/core && pnpm install --frozen-lockfile && pnpm run build && cd ../frontend && pnpm install --frozen-lockfile && pnpm run build",
  "outputDirectory": "packages/frontend/out",
  "installCommand": "corepack enable && pnpm install --frozen-lockfile",
  "env": {
    "NODE_ENV": "production"
  }
}
