{
  "buildCommand": "cd packages/core && npm run build && cd ../frontend && npm run build",
  "outputDirectory": "packages/frontend/out",
  "installCommand": "npm install --legacy-peer-deps",
  "env": {
    "NODE_ENV": "production"
  }
}
