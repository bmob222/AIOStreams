{
  "buildCommand": "pnpm -F core run build && pnpm -F @aiostreams/frontend run build",
  "outputDirectory": "packages/frontend/out",
  "installCommand": "corepack enable && pnpm install --frozen-lockfile",
  "env": {
    "NODE_ENV": "production"
  }
}
