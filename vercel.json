{
  "buildCommand": "npm run build --prefix ./packages/core && npm run build --prefix ./packages/frontend",
  "outputDirectory": "packages/frontend/out",
  "installCommand": "npm install --legacy-peer-deps",
  "env": {
    "NODE_ENV": "production"
  }
}
